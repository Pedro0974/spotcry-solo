import React from "react";
import {
    LoadingSpinnerContainer,
    Spinner
} from "./Style";

const Loading = () => {
  return (
    <LoadingSpinnerContainer>
      <Spinner />
    </LoadingSpinnerContainer>
  );
};

export default Loading;
